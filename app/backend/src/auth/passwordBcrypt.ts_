// import * as bcrypt from 'bcryptjs';

// const passwordCompare = (passowrd: string, hash: string): boolean => {
//   const comparePassword = bcrypt.compareSync(passowrd, hash);
//   return comparePassword;
// };

// export default passwordCompare;
